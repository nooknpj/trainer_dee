version: '3'
services:
    server:
        build: 
            context: ./server
        container_name: server
        ports:
            - '5000:5000'
        restart: always
    client:
        build:
            context: ./client
        container_name: client
        ports:
            - '80:3000'
        restart: always
